from pathlib import Path

# Project root
PROJECT_ROOT = Path(__file__).resolve().parent.parent

# Database path
DATABASE_PATH = PROJECT_ROOT / "db" / "database.db"

# Server name
SERVER_NAME = "EgyptAir MCP Server"